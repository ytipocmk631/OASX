<UserControl x:Class="OASX.WPF.Views.ArgsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Title bar -->
            <RowDefinition Height="Auto"/>
            <!-- Loading indicator -->
            <RowDefinition Height="Auto"/>
            <!-- Scrollable content -->
            <RowDefinition Height="*"/>
            <!-- Status bar -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Border Grid.Row="0" Padding="16,10" Background="{DynamicResource SidebarBrush}">
            <TextBlock Text="{Binding Title}"
                       FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource ForegroundBrush}"/>
        </Border>

        <!-- Loading bar -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="3"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Args content, generated in code-behind -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="12,8">
            <StackPanel x:Name="ArgsContainer" MaxWidth="720"/>
        </ScrollViewer>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="{DynamicResource SidebarBrush}" Padding="12,4">
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="11"
                       Foreground="{DynamicResource SecondaryForegroundBrush}"/>
        </Border>
    </Grid>
</UserControl>
