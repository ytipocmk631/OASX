<Window x:Class="OASX.WPF.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Configuration"
        Width="360" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New name:" FontSize="13" Margin="0,0,0,6"/>

        <TextBox Grid.Row="1" x:Name="NameBox"
                 FontSize="14" Padding="6,4"
                 Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                 KeyDown="NameBox_KeyDown"/>

        <TextBlock Grid.Row="2" x:Name="ErrorLabel" Foreground="Red" FontSize="12" Margin="0,4,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0"
                    IsCancel="True" Click="Cancel_Click"/>
            <Button Content="OK" Width="80"
                    IsDefault="True" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
